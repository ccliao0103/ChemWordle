{
  "name": "chemwordle",
  "version": "0.1.0",
  "private": true,
  "description": "ChemWordle — 化學英文 Wordle 網頁遊戲(輔大化學系班級活動)",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "vite",
    "prebuild": "node scripts/generate-config.js",
    "build": "vite build",
    "postbuild": "node scripts/postbuild-cleanup.js",
    "preview": "vite preview"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.45.0"
  },
  "devDependencies": {
    "vite": "^5.4.0"
  }
}
